@model IEnumerable<WebProgrammingProject.Models.Club>
@inject LanguageService language
@{
    ViewData["Title"] = "Index";
}

<h1>    @language.Getkey("Clubs")</h1>
          @if(User.IsInRole("Administrator")){
        <p>
            <a asp-action="Create">@language.Getkey("ClubCreate")</a>
        </p>
                }
           else if (User.IsInRole("User"))
                    {
                        <p>
                            <a asp-controller="ClubUser" asp-action="Create">@language.Getkey("JoinClub")</a>
                        </p>
                    }



<table class="table">
    <thead>
        <tr>
            <th>
                @language.Getkey("ClubName")
            </th>
            <th>
                @language.Getkey("FoundationDate")
            </th>
            <th>
                @language.Getkey("PresidentEmail")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ClubName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoundationDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PresidentEmail)
            </td>
            <td>
             @if(User.IsInRole("Administrator")){

                <a asp-action="Edit" asp-route-id="@item.Id">@language.Getkey("Edit")</a>
                        <a asp-action="Delete" asp-route-id="@item.Id">@language.Getkey("Delete")</a>

                    }



                <a asp-action="Details" asp-route-id="@item.Id">@language.Getkey("Details")</a> |

            </td>
        </tr>
}
    </tbody>
</table>
