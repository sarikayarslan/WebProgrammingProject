@model IEnumerable<WebProgrammingProject.Models.JobsUser>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Advertisement)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsAccepted)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserName.NameSurname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Advertisement.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsAccepted)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JobsUserId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JobsUserId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JobsUserId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
