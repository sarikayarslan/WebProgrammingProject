@model WebProgrammingProject.Models.JobsUser
@inject LanguageService language
@{
    ViewData["Title"] = "Create";
}

<h1>@language.Getkey("CreateJobsUser")</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="UserId" class="control-label">@language.Getkey("UserName")</label>
                <select asp-for="UserId" class ="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AdvertisementId" class="control-label">@language.Getkey("Title")</label>
                <select asp-for="AdvertisementId" class ="form-control" asp-items="ViewBag.AdvertisementId"></select>
            </div>

    @*        <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsAccepted" /> @Html.DisplayNameFor(model => model.IsAccepted)
                </label>
            </div>*@
            <div class="form-group">
                <input type="submit" value="@language.Getkey("Create")" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@language.Getkey("BacktoList")</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
